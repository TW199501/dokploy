interface:
  display_name: "Dokploy"
  short_description: "Repo-specific patterns and workflows for dokploy"
  default_prompt: "Use the dokploy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true